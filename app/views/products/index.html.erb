
<h1>Pages test</h1>

<% @products.each do |product| %>
<%= product.name %>
<%= product.description %>
<%= product.price %>

<% end %>


<% if policy(Product).create? %>
  <%= link_to "New product", user_new_product_path %>
<% end %>